# Core ML
numpy>=1.24
pandas>=1.5
scikit-learn>=1.2
xgboost>=1.7
lightgbm>=3.3

# Multi-objective optimization (for NSGA-II default optimizer)
pymoo>=0.6

# Bayesian optimization (optional, for method='bayesian')
optuna>=3.0

# DEF/SAIF parsing (standard library — no extra install needed)
# re, os, sys, pickle, collections (all stdlib)
